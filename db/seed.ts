/**
 * Función asíncrona para sembrar la base de datos con datos iniciales.
 * @returns {Promise<void>} 
 * Una promesa que se resuelve cuando se completa el proceso de simbra.
 */
// https://astro.build/db/seed
export default async function seed() {
    // TODO: Agregar lógica para sembrar la base de datos con datos iniciales.
}
